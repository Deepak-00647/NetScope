# NetScope
Python-based network packet analyzer to capture and monitor live traffic. Implemented packet inspection, protocol classification (TCP, UDP, ICMP, ARP), threat detection for port scans and SYN floods, Flask dashboard, SQLite logging, and PDF report generation.
